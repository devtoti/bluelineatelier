/**
 * Interval (seconds) for Next.js `fetch` Data Cache revalidation of Strapi project
 * list and related requests (`next: { revalidate: STRAPI_LIST_REVALIDATE_SECONDS }`).
 */
export const STRAPI_LIST_REVALIDATE_SECONDS = 3600;
